# Icons

Drop a `tray.ico` here for the system tray icon (16x16 / 32x32 multi-size ICO).
Until one is present the tray falls back gracefully (no icon, app still works).

`app_icon.ico` for the window/installer lives at `windows/runner/resources/`.
